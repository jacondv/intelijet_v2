# MatcherMsg.msg

# Keypoints ảnh 0 (flatten: [x0,y0,x1,y1,...])
float32[] keypoints0

# Keypoints ảnh 1 (flatten: [x0,y0,x1,y1,...])
float32[] keypoints1

# Confidence cho từng cặp (có thể rỗng)
float32[] confidence

